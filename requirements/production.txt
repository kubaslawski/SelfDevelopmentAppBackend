-r base.txt

# Security
django-csp==3.8

# Monitoring
sentry-sdk[django]==2.19.2

# Production server (added for compatibility with local-only Dockerfile)
whitenoise==6.8.2
gunicorn==23.0.0
uvicorn[standard]==0.32.1
